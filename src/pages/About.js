import HeroSection from "../components/HeroSection";

const About = () => {
  const headingName = {
    name: "Alpha E-Commerce",
  };
  return <HeroSection headingName={headingName} />;
};

export default About;
